# Practice solution: modularize, nest, and preview

Module call + nested `raw` container + what-if hygiene. Compare after attempting:

```bash
git diff week-14-practice-exercise-modularize-nest
```

(This solution also includes `@secure() param dbAdminPassword` from the Chapter 4 complete template; Exercise 4 focuses on that decorator.)
